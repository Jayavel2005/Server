req.on('end', () => {
        //     const bufferText = Buffer.concat(body).toString();
        //     const message = bufferText.replace('+', " ");
        //     console.log(message.split("=")[1]);

        //     fs.writeFileSync('./data.txt', message, 'utf-8');

        // })
